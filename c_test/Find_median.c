#include <stdio.h>
#define MAX(a, b) ((a) > (b) ? (a) : (b))
double findMedianSortedArrays(int* nums1, int nums1Size, int* nums2, int nums2Size) {
    int bothNums[] = {0};
    int bothNumsSize = sizeof(bothNums) / sizeof(int);
    int larger = MAX(nums1Size, nums2Size);
    for (int i = 0; i < larger; i++) {

    }
}
int main(void) {

    return 0;
}
